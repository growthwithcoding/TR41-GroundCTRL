# ============================================================================
# GROUNDCTRL BACKEND - ENVIRONMENT CONFIGURATION
# ============================================================================
#
# LOCAL DEVELOPMENT:
#   This file is used for local development only.
#
# FIREBASE APP HOSTING (PRODUCTION):
#   Sensitive variables (marked with [SECRET]) should be stored in Firebase
#   Secret Manager using the Firebase CLI.
#
#   Non-sensitive variables should be configured in backend/apphosting.yaml
#   which defines the runtime configuration and environment variables for
#   Cloud Run deployment.
#
#   To set secrets:
#     firebase apphosting:secrets:set JWT_SECRET --project groundctrl-c8860
#     firebase apphosting:secrets:set FIREBASE_PRIVATE_KEY --project groundctrl-c8860
#     firebase apphosting:secrets:set SMTP_PASSWORD --project groundctrl-c8860
#
#   See FIREBASE_SETUP.md for full deployment instructions.
# ============================================================================

# Server Configuration
PORT=8080
NODE_ENV=development
CALL_SIGN=GROUNDCTRL-01

# Firebase Configuration [SECRET - use Secret Manager in production]
# ⚠️ SECURITY WARNING: Never commit real private keys to version control
# ⚠️ The private key below is a placeholder format only - replace with actual key from Firebase Console
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nPLACEHOLDER_PRIVATE_KEY_CONTENT_HERE\nREPLACE_WITH_ACTUAL_KEY_FROM_FIREBASE_CONSOLE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_WEB_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Firebase Emulator Configuration (for local development and testing)
# ⚠️ CRITICAL: These variables are ONLY for local development
# ⚠️ DO NOT set these in production (apphosting.yaml or Cloud Run environment)
# ⚠️ The application will fail to start if these are set when NODE_ENV=production
# 
# For local development with emulators, uncomment these lines:
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# FIRESTORE_EMULATOR_HOST=localhost:8080

# JWT Configuration [SECRET - use Secret Manager in production]
# Note: Expiry values use shorthand notation supported by jsonwebtoken library
# Examples: "15m" (15 minutes), "7d" (7 days), "1h" (1 hour), "30s" (30 seconds)
JWT_SECRET=your-super-secret-jwt-key-minimum-64-characters-long-for-production
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,https://missionctrl.org

# HTTP Client Configuration
HTTP_CLIENT_TIMEOUT_MS=8000
HTTP_CLIENT_RETRY_ATTEMPTS=0
HTTP_CLIENT_RETRY_DELAY_MS=1000

# Rate Limiting Configuration
LOGIN_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX_REQUESTS=5
API_RATE_LIMIT_WINDOW_MS=900000
API_RATE_LIMIT_MAX_REQUESTS=100

# Lockout Configuration
LOCKOUT_THRESHOLD=5
LOCKOUT_DURATION_MINUTES=15
LOCKOUT_WINDOW_HOURS=1

# Logging
LOG_LEVEL=debug

# SMTP/Email Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASSWORD=your-smtp-password

# Gemini AI Configuration [SECRET - use Secret Manager in production]
# Get your API key from: https://makersuite.google.com/app/apikey
# Without this key, NOVA will use fallback responses
GEMINI_API_KEY=your-gemini-api-key-here

# URL Configuration (Production)
# For Firebase App Hosting, the backend URL format is:
# https://<backend-name>--<project-id>.<region>.hosted.app
# Example: https://groundctrl--groundctrl-c8860.us-central1.hosted.app
FRONTEND_URL=https://missionctrl.org
BACKEND_URL=https://api.missionctrl.org
